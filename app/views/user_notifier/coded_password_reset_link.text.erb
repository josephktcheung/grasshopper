We received a request to reset your Grasshopper credentials. To do so, please follow the coded link below:

<%= password_reset_form_url(@user.reset_code) %>

If you received this email in error, simply do nothing. The above link will expire at <%= @user.reset_expires_at.to_formatted_s(:mail_timestamp) %>.

Thanks!
Grasshopper
